# Compiled GSettings schema — regenerated by `make schemas` and `make pack`
src/schemas/gschemas.compiled

# Packed extension bundles
*.shell-extension.zip

# Local tooling state
.claude/


# Analyzer virtualenv, built on demand by `make shexli`.
.shexli-venv/